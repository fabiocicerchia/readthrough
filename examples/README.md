# Examples

Runnable, self-contained examples. Each one runs with a single command and
shows one clear use case.

- [`basic/`](basic/) — the whole pipeline offline and free, with `--fake`.
- [`github-actions/`](github-actions/) — a pull-request review workflow to copy
  into another repository.
